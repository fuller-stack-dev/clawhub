# ClawHub UI Proof

The exact-head local-auth Playwright scenario passed on `a092677`.

- Profile is visible directly above Dashboard.
- The compact theme row from current main remains below Settings.
- Profile links to `/user/local` and reaches the Local Owner profile.
- The browser run completed without runtime errors.
